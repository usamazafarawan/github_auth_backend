{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Milo Backend",
            "program": "${workspaceFolder}/src/api/app.js",
            "outputCapture": "std",
            "console": "integratedTerminal"
        }
    ]
}
